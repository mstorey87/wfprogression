% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fire_anu_sample.R
\name{fire_anu_sample}
\alias{fire_anu_sample}
\title{Sample LFMC or flammability from ANU THREDDS}
\usage{
fire_anu_sample(
  datetimeutc,
  sf_data,
  varname,
  allcells = FALSE,
  extract_fun = "mean"
)
}
\arguments{
\item{datetimeutc}{POSIXct datetime in UTC timezone.}

\item{sf_data}{An \code{sf} object (point or polygon).}

\item{varname}{Variable to sample: \code{"fmc"} (live fuel moisture) or \code{"flam"} (flammability).}

\item{allcells}{Logical. If \code{TRUE}, return a data frame of all raster cells intersecting \code{sf_data}. If \code{FALSE}, return a single summarised value.}

\item{extract_fun}{Function to summarise raster values (\code{"mean"} by default).}
}
\value{
An \code{sf} object input row id, extracted values, percent of cells that were NA.
}
\description{
Downloads and samples live fuel moisture content (LFMC) or flammability data
from the ANU THREDDS server for a given time and spatial feature.
}
\examples{
library(sf)
# Create a line near Canberra, Australia for 2019
my_line <- st_sfc(
  st_linestring(rbind(c(149.1, -35.3), c(149.2, -35.25))),
  crs = 4326
)
my_sf <- st_sf(id = 1, geometry = my_line)

# Sample FMC for 1 Jan 2019
result <- fire_anu_sample(
  datetimeutc = as.POSIXct("2019-01-01 00:00:00", tz = "UTC"),
  sf_data = my_sf,
  varname = "fmc",
  allcells = FALSE
)
print(result)
}
